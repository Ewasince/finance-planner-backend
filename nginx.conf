events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Базовые настройки производительности
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # Настройки Gzip сжатия
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/atom+xml;

    # Upstream для Django приложения
    upstream django_app {
        server django_app:8800;
        keepalive           20;
        keepalive_timeout   15s;
        keepalive_requests  2000;
        keepalive_time      5m;
      }

    # Основной сервер
    server {
        listen 8090;
        server_name localhost;
        client_max_body_size 100M;

        proxy_set_header          Host $host;
        proxy_set_header          X-Real-IP $remote_addr;
        proxy_set_header          X-Forwarded-For $proxy_add_x_forwarded_for;

        location / {
            proxy_send_timeout      45s;
            proxy_read_timeout      45s;
            proxy_pass              http://django_app/;
        }

        # Статические файлы - длительный кэш
        location /static/ {
            alias /static/;
            expires 1y;
            add_header Cache-Control "public, immutable";
            add_header X-Frame-Options "SAMEORIGIN";
            access_log off;
        }

        # Медиа файлы - короткий кэш
        location /media/ {
            alias /media/;
            expires 7d;
            add_header Cache-Control "public";
            access_log off;
        }

        # API и Swagger - проксируем в Django
        # location /api/ {
        #     proxy_pass http://django_app/api/;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        #     proxy_connect_timeout 60s;
        #     proxy_read_timeout 60s;
        # }

        # # Swagger UI
        # location /swagger/ {
        #     proxy_pass http://django_app/docs/;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        # }
    }
}
